# PyTorch wheels: use standard Windows CPython (python.org), not MSYS2/MinGW/UCRT64
# Python — PyTorch has no wheels for mingw*; "versions: none" there is expected.
# Official CPython 3.13+ also needs this extra index (CPU builds):
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
torch>=2.2.0
torchvision>=0.17.0
pillow>=10.0.0
requests>=2.31.0
supabase>=2.4.0
python-dotenv>=1.0.0
pydantic>=2.6.0
openai-whisper
sounddevice>=0.5.1
numpy>=1.26.0
google-genai>=0.3.0
